# Local Elastic Stack

The local development stack uses:

- Elasticsearch `9.3.x`
- Kibana `9.3.x`

Both run through Docker Compose with single-node local defaults and security disabled for local-only development.
